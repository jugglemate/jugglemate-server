# Optional fallback when localStorage and URL do not include appkey
VITE_APP_KEY=

# Optional API origin. Leave empty to use the browser's current address and port.
VITE_API_BASE_URL=

# Backend origin for Vite dev proxy (optional)
# VITE_API_PROXY_TARGET=http://localhost:8050
